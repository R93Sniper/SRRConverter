v2.0.1:
    - Initialized Defiance branch.
    - Began migration and refactoring from v1. 
    - Ported hashing function from EXE into python [CrstalDynamics/hash.py]
    - Created lazy parser for BigFile
    - Created CLI front end to hash a string [CLI/impls.py: hash_impl]
    - Created CLI front end for version
    - Created CLI front end for file extraction [CLI/impls.py]
        - Extraction by filename [extract_file_impl]
        - Extraction by hash [extract_file_impl]
        - Extraction from manifest [extract_manifest_impl]
            - Extraction from cross referenced manifest only [extract_w_manifest_cross_ref]
            - Extraction from cross references manifest + unknown files [extract_all_w_manifest_cross_ref]
    - Initialized usage information [readme.md]

v2.0.2:
    - Implemented logging system
        - Added logging messages to existing functions
    - Migrated code from __main__ into a dedicated file allowing it to be ran as a module (folder) or script (file).
    - Added `_determine_extension_from_bytes` to fingerprint filetypes with unknown name
    - Simplified hashing function to not require -s argument (`SrrConv hash <string>` instead of `SrrConv hash -s <string>`)
    - Cleaned up CLI help messages
    - Added defaults configuration.
        - Added verb for "defaults"
            - List: will list current defaults
            - Set: will update a default to a new value
        - Writing to log file will use default `log_file` if no file was specified
        - BigFile will use default `big_file` if no file was specified
        - Default output will use `out_path`
        - Hash will use default `hash_manifest` if no file was specified

v2.0.3:
    - Begin migration of srm_to_fbx
    - Added/renamed some defaults
        - Removed big_file in favor of using relative game directory path
        - added image format default
        - added sr_directory default
        - added def_directory default
    - Fixed some missed f in format strings
    - Implemented early stages of srm to fbx conversion
    - Added texture conversion
    - Renamed srm.py to srm_gen1.py to disambiguate
    - Created the FbxObject
    - Created srm_gen1_to_fbx function
    - added arguments to CLI to call conversion functions.

v2.0.4
    - Fully integrated existing functionality from v1 Gen1 SRM
    - Fully added beta support for Gen2 SRMs
        - Created CLI front end
        - Automatic detection of SRM Generation
    - Migrated defaults to it's own file [default_properies.py]
        - Changed defaults to reduce the name lengths and better describe their usage
    - Added overwrite protection
    - Updated some naming conventions to be more pythonic (and more may need to be done)
    - Added automatic game path resolution
    - Generalized srm_to_fbx to support Gen2 SRMs
    - Started adding log information
    - Minor tweak to bigfile.py allowing path to be passed as well as string
    - Renamed common.py to image.py as it only had image related functions
    - Added FbxObject and related functionality

v2.0.5:
    - Added context manager support to BigFile (__enter__/__exit__/close)
    - Added type hints throughout bigfile.py
    - Replaced TODO stubs with proper exceptions (ValueError, KeyError)
    - Fixed file handle leak in BigFile.from_file on parse failure
    - Added sanity check on file entry count with _MAX_FILE_ENTRIES constant
    - Fixed hash parameter shadowing built-in hash()
    - Updated to_stream pack order to match from_stream/dataclass field order
    - Changed bare except: to except Exception:
    - Added module, class, and method docstrings
    - Rewrote hash_str in pure Python (removed ctypes), 25 lines shorter
    - Replaced sign-check idiom with direct bit test (crc & 0x80000000)
    - Added hash_str_fast: table-driven CRC32 variant using cached lookup table
    - Renamed XOR_VALUE to _POLY for clarity
    - Added CRC table precomputation and disk caching (hash.crc_table)
    - Migrated config storage from pickle (config.defaults) to yaml (config.yaml)
    - Added automatic migration from old config.defaults on first access
    - Added list_defaults() and set_default_property() public helpers
    - Made FBX imports lazy so non-FBX commands work without the SDK
    - Cleaned up image.py: module/function docstrings, removed duplicate tga
    - Added -> bool return type hint to convert_image_data
    - Removed redundant parent-dir guard before mkdir
    - Added NORMAL_BIAS constant directly in srm_gen2.py to fix import
    - Updated help text references from config.defaults to config.yaml
    - Fixed __main__.py to allow running from SrrConv/ directory
    - Fixed _CRC_TABLE_PATH type (str -> Path) in hash.py
    - Cleaned up srm_gen1.py: BinaryIO hints, texture_type rename, MAX_BONES constant,
      warnings.warn, -> int on num_* methods, get_texture_suffixes returns list[str],
      get_normals explicit float(), index_count // 3 loop, docstrings
    - Renamed num_indices to num_triangles in both gen1 and gen2
    - Fixed SrmFile.from_file local var name (texture_palette -> material_palette)
    - Fixed get_default_property to return Path for path-like values
    - Cleaned up srm_gen2.py: moved SRM_SIGNATURE to module top, expanded abbreviated locals
    - Cleaned up fbx_object.py: added __future__ annotations, removed redundant path.parent guard
    - Cleaned up srm_to_fbx.py: added type hints, None guard on new_material, removed dead None checks
    - Fixed impls.py: type/builtin shadowing, file handle leaks, return types, arg order in gen2 call
    - Fixed srrconverter.py: missing action=store_true on --allow-overwrite, shadowed builtins, dead code
    - Fixed log.py: bare except, duplicate handler accumulation, added type hints